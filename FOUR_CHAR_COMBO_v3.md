# FOUR_CHAR_COMBO v3 — 四人连招固定脚本交接文档

> 只读本文即可接手维护。只写「结论 + 必要证据 + 位置」；无法确认的一律标 **「未验证」**。
> 基线：仓库 `C:\tool\ok-nte-src`，commit `4112808`（2026-09-16），Python 3.12 / `.venv`。

---

## 1. 定位与入口

- 固定编队（1 残虹 / 2 达芙蒂尔 / 3 伊洛伊 / 4 早雾）的严格时序固定连招，**完全绕开 planner**。`AutoCombatTask` 保持原样给其他编队用。
- 独立**触发任务**，手动启用；**不要与 `AutoCombatTask` 同开**。
- 实现：`src/tasks/trigger/FourCharComboTask.py`（继承 `BaseCombatTask, TriggerTask`）。
- 注册：`src/config.py` 的 `trigger_tasks`。任务名 `四人连招`；默认关（`_enabled=False`）；`trigger_interval=0.1`。
- 声音模板：`assets/sounds/`（`dodge.wav`、`counter.wav`、`dodge_success.wav`、`dodge_motion_1/2/3.wav`）。

---

## 2. 固定队伍与属性环

| 位置 | 类 | 中文名 | 元素（游戏 / 代码） | 文件 |
|---|---|---|---|---|
| 1 | `Zankou` | 残虹 | 咒 / `Element.RED` | `src/char/Zankou.py` |
| 2 | `Daffodill` | 达芙蒂尔 | 暗 / `Element.PURPLE` | `src/char/Daffodill.py` |
| 3 | `Iroi` | 伊洛伊 | 灵 / `Element.GREEN` | `src/char/Iroi.py`（继承 `Support`） |
| 4 | `Sakiri` | 早雾 | 咒 / `Element.RED` | `src/char/Sakiri.py`（继承 `Support`） |

属性环（仓库已有 `BaseCombatTask.element_ring`，首尾相邻）：

```
White → Green → Red → Purple → Blue → Yellow → White
光=White  灵=Green  咒=Red  暗=Purple  魂=Blue  相=Yellow
```

**咒(Red) 的邻居是灵(Green) 和暗(Purple)** → 达芙蒂尔/伊洛伊满环合切残虹会触发残虹入场技（§4.4）。

---

## 3. 最终连招流程

### 3.1 开局 opener（每场战斗一次）

入战前（未入战且残虹在场）：**只轮询检测金 E，不做任何键鼠操作**（长按由玩家自己预判敌人出现）；检测到才点 E、切达芙蒂尔，并在入战前等达芙蒂尔 Q 可用即放。

入战后固定 7 步（全程抑制战斗检测，见 §5.6）：

1. 残虹**金 E**（长按到变金 → 点 E）
2. 切**达芙蒂尔** → 点 Q（入战前已放则跳过）
3. 切**伊洛伊** → 点 E
4. 切**早雾** → 点 Q（**不放 E**, 2026-09-17 改）→ 直接切残虹
5. 切**残虹** → 双 Q → 二连（提前长按, 见 §4.2）
6. 切**伊洛伊** → Q → 浮游炮
7. 切**残虹**二连 → 进达芙蒂尔循环

**中途脱战要中止**：每步之间查一次，**连续 1.5s（`OPENER_COMBAT_LOST_GRACE`）没有任何敌人信号**就中止并清开场记忆（下一场重新从金 E 开始）。
- 判据 `_enemy_present()` = `is_boss() or find_lv() or find_target() or has_health_bar()`；**不要用 `in_combat()`**（§5.2）。
- 双 Q 之前单独再查一次，避免空放大招。

### 3.2 主循环（起点 = 伊洛伊）

- **伊洛伊**：切到后先点 **E**；Q 可放 → Q + 浮游炮；不可放 → 垫刀（在伊洛伊身上打 1.5s → 残虹二连 → 切回伊洛伊，重复到 Q 可放）。**浮游炮之后先切残虹二连，再切早雾**。
- **早雾**：Q 可放 → Q；不可放 → 同样垫刀；然后点 **E**。
- **残虹固定步骤**（双 Q 只在这里）：Q 亮 → 双 Q → 二连；Q 没亮但 CD < 2.0s → 留场连点左键等 Q 亮 → 双 Q → 二连；CD ≥ 2s → 只做二连。
- **达芙蒂尔循环**（重复到残虹环合满）：**Q 进场就放**（2026-09-17 改：她是从"残虹二连"刚切过来的，正好符合"先二连 → 马上切该角色放 Q"——Q 长动画期间白赚二连的 1.5s 残留输出；放完 Q 可控后立即切残虹）；没 Q 才放 E，然后连点普攻，在场 1.5s → 切残虹二连；场上期间 Q 转好也立即放。
- **环合 ≥0.9 不再互切**：留在残虹身上连点左键（`_stay_until_cycle_full`）直到环合满，然后切伊洛伊。
- 残虹环合满 → 切**伊洛伊** → 回主循环起点。

### 3.3 其他约定

- **可控** = Q 冷却**原始 OCR 数字**开始变小（首选）；读不到冷却数字时才退回"Q 图标由亮变灭"。**不能用 `get_cd()`**（会随时间漂移，数字挂在屏幕上就一直变小）；**也不能拿"图标变灭"当主判据** —— 实测大招一按下去图标就变灭，但人还在大招特写里（`in_team=False`），按键不生效。
- **E 切人规则**：所有角色点 E 后，观察到 E 进入 CD（已释放）即可立即切人，不必等动画收尾。按 E 前先 `_wait_in_team`（特写期间按了也白按，实测早雾放完 Q 后 E 连按 2s 全废）。
- 固定 1~4 号位；取消"检测四人是否都在队里"的保险。
- 切人确认后先停 0.1s（`SWITCH_SETTLE_TIME`）再长按。

### 3.4 四条设计原则（用户定义, 改动前先对照）

1. **残虹尽一切可能多打二连**——这是最主要的伤害来源; 且每次二连后有约 1.5s 脱手输出时间, 切其他角色上场等于白赚（剩余三人中达芙蒂尔伤害最高, 平常切她）。
2. **残虹 Q 是高爆发, 要跟在三次增伤后面尽快放**——增伤来源: ① 满环合残虹切伊洛伊(连携登场技 8s) ② 伊洛伊 E ③ 早雾 Q。
3. **稳定性**: 声音触发的闪避有最高打断优先级, 可以覆盖残虹长按; 每轮循环必放一次伊洛伊 Q + 浮游炮为全队回血充能。
4. **每名角色的 Q 都有长动画, 最好先打一个残虹二连再马上切该角色放 Q**（Q 动画期间白赚二连残留输出）; 残虹自己的 Q 之前不用垫二连（同一个人没法切）。

---

## 4. 关键机制

### 4.1 残虹二连（金 E 长按）

长按左键（**下限 0.67s / 上限 0.9s**，轮询 `Labels.zankou_skill_gold`，阈值 `GOLD_THRESHOLD=0.65`）→ 松开 → 等 0.06s → 单击左键 → 等 0.05s → 立即切人（开局金 E 是松开后点 E）。

- **`GOLD_THRESHOLD` 逐帧校准（录屏 `logs/证据.mp4`，1920x1080 窗口，同一套模板）**：

  | E 的样子 | gold 模板 | purple 模板 |
  |---|---|---|
  | **金**（条件触发） | **0.71~0.88** | ≤0.25 |
  | **白 / 蓄力** | **0.42~0.61** | ≤0.41 |
  | 紫（金 E 放完且没离场） | ≤0.18 | 0.38~0.84 |
  | 无图标（残虹不在场） | ≤0.29 | ≤0.29 |

  所以 **0.65** 能干净区分"金"和"白"。**注意：不要降到 0.45** —— 那样"白/蓄力"(0.42~0.61) 会被当成金 E（踩过一次，日志里全程 `state=gold`，看不到"变白再变金"）。
- **长按上限绝对不许回到 2s**：金 E 在 ~0.67~0.8s 出现（手动实测长按 0.87~0.99s 是"看到变金后再松手"），0.9s 还没金就是异常。
- **不许重按**：重按会白等 1~2s，限时关卡等于失败。长按起点由 §4.4 入场技预测保证。

**闪避反击后必须等"第二次金 E"**（录屏逐帧证实，2026-09-16）：

```
闪避成功 → 点按左键发动闪避攻击 → E 立刻由白变金（第一次, 不能用）
        → 直接长按 → 闪避攻击动作结束会自动发动蓄力攻击
        → 蓄力一开始, E 由金变白
        → 蓄力完成(约 0.7s), E 由白变金（第二次）→ 这时点按才稳定打出脱手攻击
```

- 录屏逐帧（`logs/证据.mp4`，30fps）实测：帧 160~174 金（0.74~0.88）→ 帧 175~193 **白**（0.42~0.61）→ 帧 194 起金（0.72~0.88）。**模板能看出来**，前提是阈值用 0.65。
- 判据（`_hold_until_gold(require_second_gold=True)`，三段）：
  1. `conf >= GOLD_THRESHOLD(0.65)` = 第一次金 E（闪避攻击带的，忽略）；
  2. `conf < 0.65` 且**持续 ≥ `SECOND_GOLD_CHARGE_MIN=0.2s`** = 白/蓄力（0.2s 是为了排除闪避命中的瞬时闪白，实测只有 0.05s，也会排掉蓄力边界上的抖动帧）；
  3. 再次 `conf >= 0.65` = 第二次金 E → 松手 → 单击左键 → 切人。
- 上限 `DODGE_COMBO_HOLD=1.9s` 只作兜底（4 次录制的手动松手 = 1.372 / 1.804 / 1.239 / 1.235s；成功的第 2 次到 1.804s 才等到第二次金，所以上限必须 >1.8s，否则会在蓄力中间收手）；到点照常收手点按，**不算异常**（日志 `dodge combo hold deadline reached`）。
- 触发路径：`_sound_dodge_success_action`（残虹分支）与**完美闪避**打断长按后的重打（`_recover_from_dodge` 之后那轮，判据是打断那一刻是否听到成功音 `_last_dodge_was_perfect`）都用这个模式；**普通路径（普攻攒出来的金 E）和普通闪避后的重打仍然第一次金 E 就点按**（`COMBO_HOLD_MIN=0.67 / MAX=0.9`）。
- 变金只是标志，目的是让残虹打完脱手攻击再切人；闪避攻击带出来的第一次金 E 点按不稳定。
- `DODGE_COUNTER_WAIT` = **0.2s**（实测"反击左键 → 开始长按"= 0.12~0.25s）。

没出金 E 时分流（**2026-09-17 16:46 改版：普通闪避重试整条删除**，理由见 §6）：

| 情况 | 判据 | 处理 |
|---|---|---|
| **长按期间闪避触发** | 长按开始后 `_dodge_heard_at` 变大（动作音/成功音回调都更新它） | **不是异常**：长按立即中断 → 点按左键触发闪避反击 → 等 `DODGE_COUNTER_WAIT=0.2s`（反击动画）→ 重打长按（完美闪避后等第二次金 E）。这条路**不消耗**超时预算 |
| **长按超时** | 0.9s（第二次金 E 模式 1.9s）到点仍无金 E，**掉不掉血都一样** | `_recover_on_daffodill()`：切达芙蒂尔**短暂登场**——她场上连点普攻的同时连按 `ZANKOU_SWITCH_KEY`(键盘 1)切回残虹，切人 CD 一好（或解除被控）**第一时间**切回（实测 <1s，**不复用 `_daffodill_window` 的固定 1.5s**）；贴人/切回确认不了 = 被控或切人 CD 没好 → `_switch_confirmed` 整体重试 `COMBO_RECOVER_SWITCH_RETRIES=3` 次 |
| 达芙 Q 时停 | 恢复登场时达芙 Q 就绪（等待切回期间必然已普攻过） | **不在场上直接放**：先切回残虹打一套二连 → 再切回达芙放 Q → 切回残虹。任何角色放 Q 期间关卡计时暂停而游戏继续，相当于白赚这套二连 |
| 超时次数超预算 | 超时 > `COMBO_RECOVERY_MAX=3` | 真状态异常 → 落盘现场（整帧 + 技能条截图）→ `disable()` 停任务 → 抛 `ZankouComboAnomaly` |

- 血条仍**连续采样**（`_health_pixels()` = 当前角色血条红条掩码非零像素数，判据 `下降 > max(4px, peak*2%)`），但**只用于日志定位**，不再改变恢复路径；恢复后按"等第一次金 E"重打（闪避攻击/蓄力上下文已不在）。
- **残虹二连残留 1.5s 是硬约束（2026-09-17 显式化）**：二连（松开+单击）后攻击动作仍持续约 1.5s，期间切回残虹长按必被动画吃掉（13:07 同机理）。`_zankou_combo`/`_zankou_combo_interruptible` **完成**单击时写 `_last_combo_finished_at`（被打断/超时/去重跳过都不写），`_hold_until_gold` 开头不足 `ZANKOU_COMBO_LINGER_TIME=1.5s` 就先补足等待再长按。正常路径实测间隔 2.5~7s，此等待为 0；它只兜"达芙 Q 施放变快/某条路径压缩时序"这类未来的坑。达芙 Q 时停分支（先吃二连→切回达芙放 Q→切回）实测 4~7s，天然满足。
- **闪避触发不算异常的原因**：闪避动画期间普攻不生效、金 E 不会出现，而成功闪避又不掉血，正好落进旧版"没掉血也没金 E"的异常判据里（旧版本这里会误抛异常停任务）。
- 长按期间的成功闪避**由长按自己收尾**：`_sound_dodge_success_action` 发现 `_holding` 为真时直接返回（只置标志），不点左键、不重打二连，避免和长按抢鼠标；长按侧中断后走上面的"点左键 → 等反击动画 → 重打"。

### 4.2 残虹双 Q

连按 Q，依次确认 4 个阶段 `enter1 → exit1 → enter2 → exit2`（每阶段要求 `is_in_team()` 稳定 0.3s，防特写抖动数错动画）→ **第 2 段动画结束（enter2 已确认 + `is_in_team()` 稳定回来）就提前长按**，不再等大招 CD 解冻——此时按住左键，控制恢复后蓄力自动开始，金 E 出现（= CD 解冻 + 蓄力 0.67~0.9s）再松手点按完成二连，与 §4.4 入场技衔接二连同一套逻辑。长按上限 = `COMBO_HOLD_MAX + ZANKOU_DOUBLE_Q_HOLD_EXTRA(3.0s)`（实测 stage=3 → CD 跳动 ≈ 3.08s，覆盖"动画尾 + 蓄力"全程）。二连长按期间警报/完美闪避打断、普通闪避重按、超时切达芙恢复全部照常生效。

> 旧判据"等 CD 原始数字变小再长按"已删（2026-09-17 晚）：那时长按还没开始，白白多等 ~2.5s；提前按住的时间不是浪费——控制一恢复蓄力立刻在跑。

### 4.3 环合

- 环合值每个角色独立；角色不在场时不变；只在残虹在场时读残虹的。
- 残虹环合满必须**直接由残虹切伊洛伊**（唯一例外：开局那次残虹切达芙蒂尔）。

### 4.4 入场技（必须提前预测）

游戏机制：**满环合**的角色切到**属性环上相邻**的角色时，新上场角色触发入场技，期间无法控制、按住不生效。

- 判据：上一任 `is_cycle_full()` **且** 属性在 `element_ring` 上相邻（含首尾环绕）。
- 计时（**2026-09-17 二次改版：长按穿过入场技，不再前置等待**）：`_entry_skill_until` 仍按"`_confirm_switch` 第一次按下切人键的时刻 + `ZANKOU_ENTRY_SKILL_WAIT(1.1s)`"记录，但长按**立即开始**（按住左键穿过入场技，期间按键不生效但按住状态保留），控制一恢复蓄力立刻开始；长按上限 = `COMBO_HOLD_MAX(0.9s) + 剩余入场技时间`，金 E 出现时间 = 入场技结束 + 0.67~0.9s。这样不用赌 1.1s 计时准不准——等短了长按落在入场技里（金 E 全空）、等长了白费输出，两种问题都消失。入场技期间的其他按键（Q/E）仍然先等入场技结束（`_wait_entry_skill_if_any`，按了也白按）。锚点必须是第一次真实按键（不能用 `_switch_to` 入口时刻，实机踩过）。常量带残虹前缀是因为入场技时长因角色而异, 此值只实测过残虹。
- 手动实测（2 次：达芙蒂尔→残虹、伊洛伊→残虹）：**切人键 → 长按 = 1.09 / 1.12s**（长按 0.93 / 0.91s，然后单击 0.10s）。
- 入场技期间按 Q 无效 → 放 Q 一律"连按到注册"。
- 日志：`four combo entry skill expected: <上一任>(元素) -> <新角色>(元素), cycle_ratio=..., wait 1.1s`。
- 代码实现**未实机验证**（§6）；`cycle_ratio()` 的判据 2026-09-17 已按实测重写（§6）。

### 4.5 声音（三条音效各司其职）

| 音效 | 模板 / 阈值 | 行为 |
|---|---|---|
| **攻击警报音** | `dodge.wav` / `counter.wav`，`Dodge Threshold=0.13` / `Counter Attack Threshold=0.12` | 只按闪避（`d`+`lshift`），不做反击 |
| **闪避成功音** | `dodge_success.wav`，`Dodge Success Threshold=0.3` | 触发反击连招 |
| **闪避动作音** | `dodge_motion_1/2/3.wav`（取 max），`Dodge Motion Threshold=0.2` | **只做通知**：确认"闪避真的触发了"（角色不可控时按 shift 不会有这个音）。**只对普通闪避可靠**：完美闪避的动作音被成功音压到 0.09~0.25（见下） |

- **优先级（用户定义，改动声音相关逻辑前先对照这里）**，数值越小优先级越高：
  1. **听到敌人攻击警报 → 强制闪避**（`d`+`lshift`）。这条由 `SoundCombatContext` 负责排动作，不占任务的节流；长按期间 `skip.check_combat=True` 不挡它，所以长按也会被打断。
  2. **听到闪避成功音 → 点按左键触发闪避反击**。当前是**残虹** → 继续长按准备二连（等"第二次金 E"，见 §4.1）；**非残虹** → 只点这一下就返回，**不等反击动画**（E/Q 都能打断反击，没必要等），被打断的代码自然接着跑（放 E / 放 Q / 切人），不强行切人、不接管主循环的二连。
  3. ~~**残虹长按期间掉血 → 尝试普通闪避**（连按 shift）~~ **已删（2026-09-17 16:46）**：掉血不再触发任何动作、也不打断长按（血条只采样记日志），长按超时走"切达芙蒂尔上场打一轮再切回"的恢复（§4.1）。闪避现在只由警报音（优先级 1）/成功音（优先级 2）驱动。
- **闪避成功反击**：残虹 → 点按左键 0.08s → 等 `DODGE_COUNTER_WAIT=0.2s`（闪避反击动画）→ 残虹二连；非残虹 → 点按左键 0.08s → 等 0.2s → 返回（见上面优先级 2）。
- **反击可被新警报打断**：长按期间再听到攻击警报 → 打断 → 闪避 → 点按左键 → 再等 0.2s → 再打二连。机制：声音线程回调 `on_sound_alert()` 只置 `_alert_interrupt`，长按轮询里检查（不依赖 `sleep_check`）。
- 长按期间听到成功闪避音时，`_sound_dodge_success_action` 只置标志就返回（`_holding` 为真），实际收尾由长按侧完成（§4.1）。
- **闪避动作音只通知不动作**：放在其它音效的节流**之前**、不占用其节流计数，只用自己的 0.3s 去重。
- **完美闪避的"闪避动作音"被成功音压得很低（2026-09-17 用进程回环录音定论）**：同一条录音里 6 次普通闪避 + 6 次完美闪避（进程回环 = `SoundListener` 实际拿到的轨），用 app 同一套滤波+匹配逐窗口打分：

  | 事件 | 动作音 | 成功音 |
  |---|---|---|
  | 普通闪避 ×6 | **0.511 ~ 0.964** | 0.075 ~ 0.234 |
  | 完美闪避 ×6 | **0.088 ~ 0.248** | **0.366 ~ 0.707** |
  | 背景（去掉事件 ±0.4s） | p50 0.066 / p90 0.092 / p99 0.114 / max 0.133 | — |

  所以：普通闪避的动作音很强（背景的 4~7 倍），完美闪避的动作音只剩 0.09~0.25（一半落在背景里）—— **动作音确实播了，只是被成功音压住/盖住**（用户假设成立；早先"完美闪避不播动作音"的说法按此修正）。结论不变：**完美闪避要靠成功音判断，普通闪避靠动作音**。现在动作音只用于"长按被闪避打断"的判定（`_dodge_heard_at`），成功音驱动成功反击。
- **阈值 0.2 已用进程回环实测验证**：普通闪避最低 0.511、背景 p99 0.114 / max 0.133 → 0.2 对背景留 1.5 倍余量、对最弱普通闪避留 2.5 倍余量，41s 录音里背景从未超过 0.2。同一条录音里警报音峰值 0.28~0.35（阈值 0.13）、成功音 0.37~0.72（阈值 0.3），与 app 内实测一致。**不要再按"设备回环录音"标定**（两者电平差约一倍，见下条）。
- **标定阈值要用进程回环录音**：`tools\record_process_audio.cmd`（GUI，跑仓库 .venv，游戏需在运行；F11 开始 / F12 停止）录的是 `SoundListener` 实际拿到的 **WASAPI 进程回环**（只含游戏进程），窗口里实时显示四个音效的当前/峰值分数，停止时写 `logs/process_audio_output.wav` + 峰值报告 `logs/process_audio_peaks_<时间>.txt`；`--analyze <wav>` 可离线复算任意录音。旧的设备回环 GUI 录音器（`tools\game_recorder_gui.py` + `tools\record_game_audio.cmd`）已删除，`.venv-tools` 也不再需要。
- 为什么不用警报音当"闪避成功"判据：两者本来就是不同的音（警报 = 敌人起手，成功音 = 闪避判定成立），实测同一录音里警报检出 17 次、成功音 15 次，时间对不上。**注意不要从"某次完美闪避前 N 秒有警报音"反推相关性** —— 那是幸存者偏差：只有先听到警报才来得及反应出完美闪避，没警报的攻击玩家也没反应过来。
- 手动实测时序（用于对齐脚本）：闪避（右键/shift）→ 长按 = 0.07~0.11s；闪避成功音 → 长按中位 0.46s；长按 0.87~0.99s；单击 0.10~0.12s。

### 4.6 检测信号结论（用什么判断什么）

| 要判断 | 用什么 | 结论 / 限制 |
|---|---|---|
| 当前角色 | `_get_current_char_detection()` | 真值来源；框架 `active health change` 会误报，不采信 |
| 大招动画 / 特写 | `is_in_team()` | **特写期间 False**（多场日志证实）；状态会抖动 → 判定要"稳定 0.3s" |
| Q 冷却是否真的在计时 | 原始 OCR 数字（`refresh_cd()` + `cds[idx]["ultimate"]`） | `get_cd()` 会随时间漂移，不能用来判断（§5.3） |
| 环合是否满 | `is_cycle_full()` / `cycle_ratio()` | **顶部白环厚度 / 整圈中位厚度**（≥0.5 算满）。环合像倒水一样从下往上涨，未满时缺口在 12 点方向；"差一点点(98%)"时顶部虽然碰到白色，但只剩一条 ~0.3px 的薄白边，所以必须比厚度而不是"有没有白像素" |
| 玩家是否掉血 | `_health_pixels()` | 当前角色血条红条掩码非零像素数；必须**多次采样**取下降 |
| 敌人还在不在 | `_enemy_present()` | `is_boss() / find_lv() / find_target() / has_health_bar()`；不用 `in_combat()` |
| 闪避是否真的生效 | **闪避动作音**（普通闪避）/ **闪避成功音**（完美闪避） | 角色不可控时按 shift 不会有动作音；完美闪避的动作音被成功音压到 0.09~0.25，只有成功音（0.37~0.71）可靠（§4.5） |
| 技能是否可用 | `box_highlighted()` + `has_cd()`（即 `available()`） | 仅**当前角色**可靠；非当前角色模板匹配常 `conf=0.0` |
| 金 E 是否出现 | `find_one(Labels.zankou_skill_gold)` | 原图颜色模板匹配、无专用滤镜；**阈值 0.65**。录屏逐帧：金 0.71~0.88、白/蓄力 0.42~0.61、紫 ≤0.41、无图标 ≤0.29 |

---

## 5. 已验证的可靠性规则（最容易重新踩的坑）

1. **切人检测不可靠 → 自研确认 + 关键节点复查**：框架 `active health change` 会误报（残虹没上场却报成功）。用 `_get_current_char_detection()` 图像确认，重按切人键直到目标高亮**连续稳定 `SWITCH_CONFIRM_STABLE=0.15s`**（过滤切换动画里的瞬态高亮），超时 3s；跳过框架切人自带的左键点击。
   **但仍会误判**：实测 21:01 早雾→残虹 日志打了 `switch confirmed -> Zankou in 0.83s`，场上其实还是早雾（随后二连长按期间金 E 模板全程 `conf=0.000`，触发"没掉血也没金 E"异常停任务）。所以**关键节点必须复查**：`_verify_current(char)` 重新检测 2 次；二连"没掉血也没金 E"时先复查当前角色，不是残虹就**重切再打一轮**，只有确认人在残虹身上还不出金 E 才抛 `ZankouComboAnomaly`。
2. **不要用 `in_combat()` 判断"敌人还在"**：关卡切换/敌人刚死时它会因异步检测 pending 或"重新索敌成功"而继续返回 True（日志 `miss=0` 且 `is_boss/lv/target/health_bar` 全 False 却仍 in_combat）。用 `_enemy_present()` 的原始信号。
3. **`get_cd()` 不能判断"冷却是否开始计时"**：它是"OCR 值 − 自快照以来的时间"，冷却数字只要挂在屏幕上就一路变小（即使游戏冻结冷却）。游戏在 Q 动画期间把冷却数字**冻结在满值**（实测 Q#2 后 3.1s 原始值仍是 `20.0`）→ 必须看**原始数字**是否变小。
4. **不要"动画一结束就长按"**：双 Q 后必须等 §4.2 的两个条件；切人/入场技期间按住不生效，必须等 §4.4 的 1.1s。
5. **长按 ≤0.9s、不许重按**（§4.1）。
6. **紧输入序列要抑制战斗检测**：`check_combat()` 会触发重新索敌（`middle_click`）打断输入。开场/主循环用 `_suspend_combat_check()` 覆写 `check_combat`；更细的循环用 `skip_sleep_checks(check_combat=True)`。**注意 `BaseChar.click_skill()` 内部的 `sleep` 会走 `sleep_check → check_combat`，`skip_sleep_checks` 覆盖不到** → 本任务不用 `click_skill()`。
7. **`click_skill()` 会阻塞在场窗口**（连按 E 到技能不可用，最长 15s，把窗口计时卡死）→ 一律用"E 进 CD 即返回"的非阻塞版本。
8. **Q 会被入场技/切人动画吃掉** → Q 一律"连按到注册"，不要只按一次。
9. **金 E 检测阈值必须 0.65**：`find_one(Labels.zankou_skill_gold)` 是原图颜色模板匹配、无专用滤镜。录屏逐帧：金 0.71~0.88、白/蓄力 0.42~0.61、紫 ≤0.41、无图标 ≤0.29。**降到 0.45 会把"白/蓄力"当成金 E**（踩过一次，日志里全程 `state=gold`，看不到"变白再变金"）。
10. **队友 Q 模板不稳**：非当前角色的 `ultimate_available(index)` 常 `conf=0.0`；能读到当前角色时优先用当前角色的 `available`。
11. **声音模板必须从"干净录音"里切**：闪避动作音混在音乐里时相关值主要来自音乐（从带音乐的录音切模板 → 闪避点最低分低于背景最高分，完全不可用）。素材要"只按闪避、不移动"（边移动会把脚步声叠进来）。
12. **不要在轮询热路径里直接 `log_info`**：`Iroi._wait_ultimate_unfreeze` 会以 ~250 次/秒轮询，已节流到每角色 1 条/秒。
13. **外置代码限制**：`custom_chars/external_chars` 只能覆盖单个角色，做不了跨角色固定脚本 → 必须独立任务 + 跑源码。
14. **打包版会被更新覆盖**：改 `data/apps/.../repo` 无效。

---

## 6. 未验证 / 已知限制

**未验证（代码已写，但没跑过实机 / 样本不足）**
- 入场技预测（判据 + 1.1s 计时）：手动时序已验证，代码路径没跑过实机。**锚点已修（2026-09-17）**：`_entry_skill_until` 从"_switch_to 入口时刻"改为"`_confirm_switch` 第一次按下切人键的时刻"（`ENTRY_SKILL_WAIT` 更名 `ZANKOU_ENTRY_SKILL_WAIT`，入场技时长因角色而异；Q 特写等待拆为独立的 `Q_CUTSCENE_START_WAIT`）。**长按策略已改（2026-09-17 晚）**：不再前置等待 1.1s，长按直接开始、按住穿过入场技，上限按剩余入场技时间延长（见 §4.4）——等短/等长两类时序问题一并消除。
- `cycle_ratio()` 曾出现"伊洛伊(灵)→残虹(咒)"相邻却没预测出入场技（13:07 那次异常的直接原因）。**已修（2026-09-17）**，最终判据 = **顶部白环厚度 / 整圈中位厚度 ≥ 0.5**：
  - 旧实现取"12 点 / 6 点白像素密度比"：环合过半后上下两点都白、比值饱和在 ~1.0（分不出 50% 和 100%），而 `>0.9` 才算满又要求两点密度几乎相等，线宽/AA 略有差异就误判未满。
  - 几何实测（`tools\ring_detect.cmd`，点圆环中心自动找白环内外圈）：2560x1440 空间下圆心 (976.7, 1348.6)、内半径 28.7px、外半径 31.3px，环厚 ~2.6px；app 现有裁剪框中心 (977, 1349) 是对的，问题只在判据。
  - 白度实测：暗环 36~41、白环 238，阈值 200 分得很干净。
  - 分档实测：`0.4.png`（目测 40%，覆盖 45.4%）顶部整段暗；`0.75.png`（目测 75%，覆盖 67.9%）顶部整段暗；**`0.98.png`（目测"几乎满"）顶部虽然碰到白色，但只有 1~2 个径向采样点（~0.3px）的白边，整圈是 ~2.5px**；满环顶部厚度与整圈一致。
  - 所以判据取"顶部厚度 / 整圈中位厚度"（自校准，不依赖分辨率/阈值），`is_cycle_full()` = `>= 0.5`。实测：`0.98.png` → 0.25（不满）、`1.png` → 0.98（满）。
  - 单测 `tests\TestCycleRatio.py`：合成 0/40/75/90% 都不算满、满环算满，另有一条读 `screenshots\0.98.png`/`1.png` 的真实截图回归（截图不在时自动跳过）。
- 掉血阈值（`下降 > max(4px, peak*2%)`）：只有 1 个样本（满血 `health_peak=306` 像素、0.9s 采到 13 次）。
- ~~闪避重试（连按 shift + 闪避动作音确认）与异常停止路径的实机表现~~ **机制已删（2026-09-17 16:46，见 §6）**；新的实机验证点是 `_recover_on_daffodill`：日志看 `zankou gold E timeout (n/3), recover on daffodill` 之后是否正常切达芙/切回并重打成功。
- **闪避打断长按后的恢复**（点左键 → 等 `DODGE_COUNTER_WAIT=0.2s` → 重打长按）：代码路径没跑过实机；0.2s 是实测的（7 次"反击左键 → 开始长按" = 0.12~0.25s）。
- **闪避后的"第二次金 E"（代码已写，未实机验证）**：机制与逐帧证据见 §4.1（金 → 白/蓄力 ≥0.2s → 金）。上限 `DODGE_COMBO_HOLD=1.9s` 只作兜底，到点照常收手点按、**不算异常**（日志 `dodge combo hold deadline reached`）。阈值 0.65 与三段判据都来自录屏/手动实测，但**代码路径本身没跑过实机**；真机看日志 `second gold:` 三段是否都出现，没出现也能靠 1.9s 兜底收手。另外 4 次手动录制的第 3 次失败，从 E 状态看与成功的几次完全同构（同样金→白→金、同样在 white 里松手），所以那次是游戏侧失误，不是判据问题。
- `SWITCH_SETTLE_TIME=0.1` 不够长：切人确认后角色仍在切人动画里（实测 13:07 切残虹后 0.13s 长按 → 0.9s 全落空）。现在靠"超时 → 达芙蒂尔恢复一轮"兜住（2026-09-17 16:46 起），代价是每次约 0.9s + 恢复窗口；以后可以改成"等角色真的能出手"的信号（目前没有可靠的）。
- 二连"松开 → 单击"间隔 0.06s（手动实测 0.14~0.31s，未验证是否偏短）。

**已知限制（暂不改）**
- **当前角色头像标记误判（2026-09-17 18:29 实机，用户决定先记下不修）**：用户确认当前角色的头像标记对**所有角色**都是粉红亮环（不是按元素变色）。疑似该关卡（Lv.88 胶卷-MANISH，全场红幕布/粉红背景）的暖色背景渗进头像裁剪区，让 active-marker 检测连续 13s 报错（切伊洛伊 want 2, got 0→1→-1，伊洛伊在场上却确认不了），最终按"切人重试耗尽即抛异常"停任务（截图 `four_combo_combo_anomaly_20260917_182953.png`）。用户判断 99% 的情况正常，暂不修；若高频复发，方向是排除头像裁剪区里的背景暖色，或按亮度/形状区分"当前角色环"与"大招就绪环"。
- **切人确认在"新上场角色入场技期间"可能整段失明（2026-09-17 18:00 实机）**：开场第 4 步切早雾已生效（用户亲眼看到早雾上场），但 `_confirm_switch` 连续 3s 检测报 `want 3, got 1 (reason=active_marker)`（报的是达芙蒂尔），随后裸继续 → 在早雾入场技动画里轮询她的 Q（`ult:False conf=0.042`，图标是灰的）白等 `Q_READY_TIMEOUT=5s`、E 再白按 2s，早雾全程发呆约 20s 直到用户手动关游戏。**已修（2026-09-17）**：① 开场全部切人改走 `_switch_confirmed`（整体重试 + `_verify_current` 复查，检测失明时下一轮重试自然确认）；② 任一关键切人重试后仍失败 → 落盘现场 + 停任务 + 抛 `ZankouComboAnomaly`（**开场流程不允许跳步**, 用户明确要求, 宁可停下来人工看）；③ `_cast_q` / `_skill_until_registered` 统一先 `_wait_entry_skill_if_any()` 等入场技结束再按键（每次只消费一次；长按例外——直接按住穿过入场技, 见 §4.4）。关游戏瞬间报的 `No box found for category Labels.ult_ready` 是框架在无画面时的正常异常，不用管。
- 双 Q 的 `exit2` 基本确认不了（超时 8s < 全程 ~11s），二连时机不依赖它。
- 早雾 E 经常放不出来（日志 `Sakiri skill not registered within 2.0s`，`lit=0` / `in_team=False`）。根因是她刚放完 Q 还在大招特写里（`in_team=False`），而按键在特写期间不生效。**已修**：`_wait_controllable` 改成等 Q 冷却**原始数字**开始变小（"图标由亮变灭"只作没有冷却数字时的兜底 —— 实测大招一按下去图标就变灭，但人还在特写里），`_skill_until_registered` 也先 `_wait_in_team` 再按 E，特写期间不浪费按键。
- 切人确认**两个方向都会错**：漏判（`four combo switch not confirmed, want X, got -1`）和误判（21:01 早雾→残虹 误报 confirmed，见 §5.1）。误判已由 `_verify_current` + 二连前的复查兜住；漏判仍靠重按 3s 兜。
- **主循环二连会和"闪避成功反击二连"撞车（21:14 异常的直接原因，已修）**：完美闪避发生时主循环往往已经切到残虹、正准备打二连，此时声音路径先接管打了一套反击二连（用掉了 E），主循环醒来又打一套 → 二次长按只能看到"上一次残留的金 E"，而它落在 `COMBO_HOLD_MIN=0.67s` 之前被有意忽略 → 报 `NO_GOLD`；这 0.9s 里角色站着挨打 → `damaged=True` → 走闪避重试 → 角色正被连击闪不出来 → 抛异常停任务。21:13 同一模式也发生过，但残留金 E 撑过了 0.67s 就被当成金 E 点掉了（等于白点一次），所以这是个**靠时序的抽奖**。
  **修法**：声音路径在"确实点出那一套"时写 `_last_zankou_combo_at`（只写在 `_zankou_combo_interruptible` 返回 False 那一处，即真的打了连招+点按；`_holding` 早退和非残虹分支**不写**，非残虹那条本来就要靠主循环去切残虹打二连）；`_zankou_combo` 开头 `_zankou_combo_recently_done()` 命中就只打日志跳过（窗口 `ZANKOU_COMBO_DEDUP_WINDOW=2.5s`，实测撞车间隔 0.07~0.14s，加上切人 settle 0.1s + 入场技预测 1.1s 也够；残虹二连自然间隔由 E 冷却决定 ~10s，不会误伤）。**注意不要写在 `_zankou_combo_switch` 里直接 `return`**：那个函数的返回值是"下一个切谁"，早退会让 `_loop_once` 拿到 `None` 打断环合流程；写在 `_zankou_combo` 里才是安全点。
- **"闪避动作音"在游戏里从未触发过（2026-09-17 定论，模板/捕获/算法都没问题）**：两个会话 0 次 `Dodge MOTION TRIGGERED`。用进程回环录音（`tools\record_process_audio`）实测后确认：**普通闪避 0.511~0.964、背景 ≤0.133**，捕获和匹配都正常；游戏里没触发的原因是 ① 警报驱动的那几次闪避全是**完美闪避**，它的动作音被成功音压到 **0.088~0.248**（落在背景附近），而成功音有 0.366~0.707 —— 即"这个音确实播了，但只有成功音可用"；② 21:14 那次连按 shift 期间角色正在挨打（`damaged=True`、血量在掉），**一次闪避都没出来**，自然没有动作音；③ 旧阈值 0.3 高于完美闪避的动作音水平。已做：阈值 0.3→0.2（实测对背景留 1.5 倍、对最弱普通闪避留 2.5 倍余量）、触发日志与去重不再依赖回调是否接上、监听日志改成记录**区间峰值**（`dodge_motion_score: 0.0412/peak 0.8130`）。
- **普通闪避重试（连按 shift + 动作音确认）已整体删除（2026-09-17 16:46 实机定论）**：16:46 开场第 7 步切回残虹打二连时撞上敌人连击，警报闪避反复打断长按，主路径 3 次重试预算全被烧光；第 3 次重试的 sleep 里又重入执行了完整的成功反击（6s，内含它自己的多轮长按/闪避），反击内部 `_recover_from_dodge` 清掉了成功音标志 → 反击明明已把二连打出来并写了 `_last_zankou_combo_at`，`_dodge_until_triggered` 醒来却查不到任何标志 → `NOT_TRIGGERED` → 预算耗尽 → 误报 `gold E still missing after 3 dodge attempts` 停任务。这是"主路径闪避重试 vs 声音路径成功反击"的撞车（21:14 的同族第三种形态）。用户决策：该机制收益只是"确认角色能动"，负担太大 → 删除。现行为：**掉血不打断长按、不触发闪避**；长按超时（掉不掉血都一样）→ `_recover_on_daffodill` 切达芙蒂尔**短暂登场**（连点普攻 + 连按 `ZANKOU_SWITCH_KEY`，切人 CD 一好第一时间切回，<1s），贴人确认不了 = 被控，`_switch_confirmed` 整体重试 `COMBO_RECOVER_SWITCH_RETRIES=3` 次；达芙 Q 就绪时先吃一套二连再切回达芙放 Q（利用大招时停，关卡计时暂停而游戏继续）；超时超 `COMBO_RECOVERY_MAX=3` 才异常。被闪避/警报打断的长按**不消耗**超时预算。

---

## 7. 修改时必须遵守的规则

1. 先读本文再动代码；只改本任务相关文件，不顺手重构。
2. 保持"只通过 UI / 系统输出交互"的边界（截图、OCR、模板、声音、窗口 API、普通键鼠）。
3. 长按 ≤0.9s、**不许重按**；长按起点必须由 §4.2 双 Q recovery 或 §4.4 入场技预测保证，且距上次二连完成 ≥ `ZANKOU_COMBO_LINGER_TIME`(1.5s, `_hold_until_gold` 开头显式补足)。
4. 冷却判断用**原始 OCR 数字**，不用 `get_cd()`。
5. 敌人是否还在用 `_enemy_present()`，不用 `in_combat()`。
6. 不采信框架的 `active health change` 切人判定。
7. **长按结果只有三种合法出口**：金 E（正常收手点按）/ 被闪避打断（`_dodge_heard_at` 变大 → 点左键 → 等反击动画 → 重打长按，不能抛异常；声音侧反击路径在 `_holding` 为真时必须让位）/ 超时（掉不掉血都一样 → 切达芙蒂尔恢复，不许立即异常，也不许再用连按 shift 的"普通闪避重试"）。
8. 声音：新增音效按"闪避动作音"的模式加（独立阈值 + 独立去重 + 不占用其它音效节流）；模板必须从干净录音切。
9. 改完必须跑 `py_compile` + `unittest discover -s tests -p "*.py"` + `ruff`，然后 `git add` + `git commit`。
10. 同步更新本文的对应章节（流程 / 机制 / 未验证项）。

---

## 8. 关键源码路径

- 任务实现：`src/tasks/trigger/FourCharComboTask.py`（本脚本全部逻辑）。
- 战斗基类：`src/combat/BaseCombatTask.py`（`element_ring` / `cycle_ratio()` / `_apply_sound_config` / 切人与声音接线）。
- 脱战判定链：`src/combat/CombatCheck.py`（本任务无额外判定）。
- 当前角色检测：`src/utils/current_char_detector.py` + `src/tasks/mixin/CharUIMixin.py`。
- 角色实现：`src/char/Zankou.py`、`Daffodill.py`、`Iroi.py`（浮游炮 `_wait_ultimate_unfreeze`）、`Sakiri.py`；角色基类 `src/char/BaseChar.py`。
- 声音子系统：`src/sound_trigger/SoundListener.py`、`SoundCombatContext.py`、`DodgeCounterTrigger.py`；阈值配置项 `src/config.py` 的 `Sound Trigger Config`。
- 日志（排查优先读前两个，都是本地生成物、不要提交）：
  - `logs/four_combo.log`（流程日志，含落盘截图 `four_combo_<tag>_<时间>.png`）
  - `logs/four_combo_actions.log`（键鼠时序日志）
  - `logs/skill_state.log`（录制器写的 E 状态：变化 + 每秒采样；`logs/e_state/*.png` 是状态变化时的 E 图标裁图）
  - `logs/ok-script.log`（全量，午夜轮转保留 7 天）
- 环境与命令：Python 3.12 / `.venv`；改完跑
  `.\.venv\Scripts\python.exe -m py_compile <file>`、`.\.venv\Scripts\python.exe -m unittest discover -s tests -p "*.py"`、`uv run --with ruff ruff check <file>`。
