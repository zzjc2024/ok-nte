# 送礼 / 好感度功能改造方案（v3，待确认）

目标：在现有「羁遇赠礼」标签页上新增
1. **库存预警看板**：记录每日送礼消耗，自动算「当前库存还能送几天」。
2. **好感升级计算器**：设置主角特技等级（1-5，5% 加成阈值 = 特技等级 + 3），
   在角色单独页面选「后续买哪种礼物」，自动算「还要买多少个才能到目标等级」。

本文只是方案，未动代码。末尾「待确认问题」需要用户补充信息后再开工。

---

## 1. 现有实现侦察

| 位置 | 作用 | 关系 |
| --- | --- | --- |
| `src/gifts/GiftDb.py` | JSON 持久化（`schema_version=1`），`profiles` 字典 | **扩 schema 到 v2** |
| `src/gifts/GiftManager.py` | 单例；profile 增删改查、帧图片落盘 | 复用，新增字段读写走这里 |
| `src/gifts/layout.py` | 赠礼页坐标比例（已核对：5×2 主网格、列距 0.0651、行距 0.1351 **仍正确**） | 扩展新框 |
| `src/tasks/daily/GiftTask.py` | 自动赠礼；已能 OCR 等级「10」判满级、OCR `(\d+)/3` 次数 | **接入：读快照 + 记消耗** |
| `src/ui/GiftManagerTab.py` | 单页编辑器（左角色列表 / 右设置卡 + 礼物优先级卡） | **加两块 UI** |
| `src/config.py` | `GiftTask` 已注册日常任务，`GiftManagerTab` 已注册标签页 | 不用改注册 |

---

## 2. 截图实测（`C:\tool\ok-nte\data\apps\ok-nte\working\screenshots\*.png`，1920x1080 客户区）

赠礼页**直接显示了全部需要的数据**：

- **好感度等级**（1/2/3/4/8/9/10）+ **当前级经验/需求**（`1550/12000`、`500/2000`…）。
- **每个礼物格的经验值**（心形数字：实测 `100` / `400`）+ **库存数量**（角标：2/28/6/3/65/50/126/510…）。
- **特殊礼物标记**：选中带 `CD` 角标的礼物时，底部变「该礼物为特殊礼物，不进入每日赠礼次数限制」。
- **每日剩余次数**：底部横幅「今日还能赠送10次礼物」；按钮「赠送3/3」。

### 2.1 经验表（实测 vs 用户给的表）——**需要修正**

| 升级 | 实测截图 | 用户给的表（按索引） |
| --- | --- | --- |
| 1→2 | **500** | index0=100 / index1=400 |
| 2→3 | **1000** | index2=1000 ✅ |
| 3→4 | **2000** | index3=2000 ✅ |
| 4→5 | **3500** | index4=3500 ✅ |
| 5→6 | ？（缺截图） | index5=5000 |
| 6→7 | ？（缺截图） | index6=7000 |
| 7→8 | ？（缺截图） | index7=9000 |
| 8→9 | **12000** | index8=12500 ❌ |
| 9→10 | **16000** | index9=16000 ✅ |
| 10 | 满级（溢出显示 `220`） | — |

推断：用户表是按「等级 = 索引」写的，只是 **L1 应为 500、L8 应为 12000**，且多写了一个没用的
index0=100。**需要用户确认 L5/L6/L7，最好补 5/6/7 级截图。**

### 2.2 已确认的规则

- **每日次数**：**每角色 3 次 + 全局 10 次**，两个上限都要卡。
- **每日额外经验（约会）**：不吃 5% 加成；**每个角色页面各自一个「勾选框 + 数字输入框」**
  （默认 200），勾上就认为该角色每天固定获得这么多经验。这样绕开「每天只能约会一个角色」的麻烦。
- **礼物列表比一屏长**，但自动赠礼本来只用首屏可见礼物，所以**不考虑滚动**。

---

## 3. 旧代码（`000送礼好感计算器.py`）审查

核心升级循环（`gain_exp`：按当前等级逐次加经验、边加边升级；5% 判定用「送礼前等级」）**逻辑正确，可直接移植**。要修 4 处：

1. **5% 阈值写死 8**（第 99 行）→ 参数化为 `特技等级 + 3`（1→≤4 … 5→≤8）。
2. **手写信(2000) 在 Day 0 一次性用完，绕过每日上限**（第 140-145 行）→ 按新规则重写。
3. **每日约会 +200 也乘了 1.05**（第 153 行）→ 改为不吃加成（已确认）。
4. **展示的「总经验缺口」没算加成**（第 188 行）→ 偏高。

另：`int(base * coef)` 是截断；单角色、无持久化、无历史。

---

## 4. 方案

### 4.1 数据模型（`GiftDb` v2，带迁移）

```jsonc
{
  "schema_version": 2,
  "settings": {
    "protagonist_skill_level": 1,     // 1-5, 5% 阈值 = 该值 + 3
    "daily_gift_limit_per_char": 3,   // 每角色每日上限
    "daily_gift_limit_global": 10,    // 全局每日上限
    "exp_table": { "1": 500, "2": 1000, "...": 0 }
  },
  "profiles": {
    "gift_xxx": {
      // ... 现有字段不动 ...
      "bond_level": 8,           // 羁遇等级（OCR 预填, 可手改）
      "bond_exp": 1550,          // 当前级已存经验
      "target_level": 10,        // 目标等级
      "buy_tier": 100,           // 库存耗尽后买哪种档位
      "daily_extra_exp": 200,    // 每日额外经验（约会）
      "daily_extra_enabled": false
    }
  },
  "gift_catalog": {              // 新增: 礼物目录（按图标模板识别）
    "gift_abc": { "exp": 400, "special": true, "unlimited": false, "name": "..." }
  },
  "stock": { "gift_abc": 2, "gift_def": 65 },   // 新增: 每件礼物当前库存（OCR 快照）
  "stock_snapshot_at": "2026-09-22T19:56",
  "ledger": {                                   // 新增: 每日消耗
    "2026-09-22": { "sent": { "gift_abc": 2 }, "characters": { "残虹": 3 } }
  }
}
```

v1 库读入时自动补默认值并升版本，`profiles` 原字段不动。

### 4.2 礼物目录：按**图标模板**识别（关键设计）

**实测发现（重要坑）**：同一件礼物在不同角色页的**格位不同、首屏可见集合也不同**。

| 角色 | 首屏主网格（前 5 格） |
| --- | --- |
| 残虹 | 票券400×2、饮料100×28、菜篮100×6、药瓶100×3、棉花糖400×65 |
| 早雾 | 兔耳400×1、信400×2、礼盒100×510、奶茶100×7、棉花糖400×65 |
| 阿德勒 | 票券400×2、饮料100×33、零食100×3、牛奶100×3、棉花糖400×65 |

所以**不能把「首屏各礼物数量加起来当库存」**：会少算，且每个角色算出来还不一样。

对策：给礼物建**目录**，用**图标模板**（复用现有 `find_one`）做唯一身份：
- 首次采集角色时，顺带把 10 个格位的图标存成模板，并 OCR 出各自的「经验值」；
- 跨角色用模板匹配去重 → 得到全局礼物目录（`gift_id → exp`）；
- 读库存时，每格「模板匹配到 gift_id + OCR 数量」→ 写 `stock[gift_id]`。

这样跨角色/跨页都能对上，前后差分也才有意义。

### 4.3 纯计算核心（新 `src/gifts/AffinityCalculator.py`）

```python
EXP_TABLE = {1: 500, 2: 1000, 3: 2000, 4: 3500, 5: 5000, 6: 7000, 7: 9000, 8: 12000, 9: 16000}
MAX_LEVEL = 10
def bonus_threshold(skill_level) -> int: return min(8, skill_level + 3)
def gift_gain(base_exp, level, threshold) -> int: ...   # 带/不带 ×1.05

def simulate_character(...) -> CharacterPlan   # 需几天 / 各档消耗 / 各档需买 / 逐日明细
def simulate_all(profiles, settings, stock) -> GlobalPlan
```

- 每日流程：先加「每日额外经验」（不吃加成）→ 再按**每角色 3 次、全局 10 次**送礼；
  库存按「高经验优先」取用，耗尽按该角色 `buy_tier` 购买。
- 全局 10 次的分配顺序：按角色列表顺序，逐个给到各自上限或全局用尽（**待确认**是否要能调优先级）。
- 产出「各档每日消耗 / 库存耗尽在第几天 / 全部达标需几天 / 需买总数」。

### 4.4 消耗记账：送礼前后差分（用户方案 + 修补）

用户方案：送完当前角色后再 OCR 一次，算各礼物数量的变化 → 得出消耗 → 推算还能撑几天。

**方向对，但有 5 个坑要补：**

1. **跨角色对不上**：首屏礼物集合不同 → 必须先有「图标模板目录」（见 4.2），
   差分要**按 gift_id** 而不是按格位。
2. **覆盖范围**：差分只覆盖「本次实际送的角色」。一次跑多角色时，建议**整个 run 前后各读一次**
   （最省），或每个角色送完各读一次（更细）。
3. **「变化」≠「每日速率」**：某天只送了一部分角色、或隔几天没跑，速率就不准。
   建议：天数以**计算器的计划速率**为准，差分实测作为对照展示（避免用一次异常值推天数）。
4. **不要走「更新当前角色」按钮**：那个按钮会 recapture 并可能改到 `blocked_slots`（有副作用）。
   应新增独立的**「刷新库存」**入口：只读赠礼页、只更新 `stock`/`gift_catalog`，不动 profile。
5. **无限礼物**：角标是 `∞`（现有 `unlimit_gift` 已能识别）→ 库存记为无限，不参与「撑几天」。

记账流程：
```
刷新库存:  截当前赠礼页 -> 每格模板匹配 gift_id + OCR 数量 -> stock / snapshot_at
跑完赠礼:  开始前 stock 快照 -> 跑完再读 -> 差 = ledger[今天].sent -> 刷新 stock
```

### 4.5 UI（扩展 `src/ui/GiftManagerTab.py`，不新建标签页）

- **库存预警看板**（左栏下方紧凑卡）：每件礼物「图标 / 经验 / 库存 / 每日消耗 / 可撑天数」，
  加一行「全部达标还需 N 天，需买 X 个」，以及「刷新库存」按钮。
- **好感升级计算器**（右栏）：
  - 设置卡新增「主角特技等级」下拉（1-5）。
  - 角色卡新增「当前等级 / 当前经验 / 目标等级 / 后续购买档位」+「从游戏读取」按钮，
    以及「每日额外经验：☑ + 数字框」。
  - 结果区（只读文本，仿旧版报告）：还需 N 个礼物、需 D 天、加成是否生效。
- 字符串走 `self.tr()`，同步 i18n。

---

## 5. 实施步骤（建议 7 步）

1. **数据模型 v2 + 迁移 + 单测**（`GiftDb` / `GiftManager`：新字段、目录、库存、账本）。
2. **计算核心 + 单测**（`AffinityCalculator`：经验表、5% 阈值、单角色、多角色共享库存、
   每角色 3 + 全局 10、剩余天数）。
3. **礼物目录 + 赠礼页读取器 + 单测**（图标模板去重、OCR 经验/数量/等级；
   用 7 张截图做本地回归，缺图自动跳过；截图不入库）。
4. **`GiftTask` 接入 + 单测**（前后快照差分记账、刷新库存；可选自动读等级）。
5. **UI 库存预警看板**。
6. **UI 好感升级计算器**（每角色）。
7. **i18n + 文档 + 全量 `unittest discover`**。

（5、6 可合并；3、4 可合并。）

---

## 6. 待确认问题

1. **经验表**：是否按上面修正为 `L1=500 … L8=12000`？**L5/L6/L7 是多少**（能否补 5/6/7 级截图）？
2. **礼物档位**：经验值只有 `100` 和 `400` 两种吗（旧代码还有 200/2000）？
   哪些是「特殊礼物（不占次数）」？哪些能在商店买到、怎么兑换？
3. **全局 10 次的分配**：默认按角色列表顺序逐个给满 3 次，可以吗？要不要能调优先级？
4. **天数口径**：以「计算器计划速率」为准、差分实测作对照，可以吗？
5. **礼物目录的模板**：同意用「图标模板 + 经验值」给礼物建唯一身份吗？
