# 送礼 / 好感度功能改造方案（v2，待确认）

目标：在现有「羁遇赠礼」标签页上新增
1. **库存预警看板**：记录每日送礼消耗，自动算「当前库存还能送几天」。
2. **好感升级计算器**：设置主角特技等级（1-5，5% 加成阈值 = 特技等级 + 3），
   在角色单独页面选「后续买哪种礼物」，自动算「还要买多少个才能到目标等级」。

本文只是方案，未动代码。末尾「待确认问题」需要用户补充信息后再开工。

---

## 1. 现有实现侦察

| 位置 | 作用 | 关系 |
| --- | --- | --- |
| `src/gifts/GiftDb.py` | JSON 持久化（`schema_version=1`），`profiles` 字典 | **扩 schema 到 v2**（全局设置 + 每角色等级 + 消耗账本） |
| `src/gifts/GiftManager.py` | 单例；profile 增删改查、帧图片落盘 | 复用，新增字段读写走这里 |
| `src/gifts/layout.py` | 赠礼页坐标比例（已核对：5×2 主网格、列距 0.0651、行距 0.1351 **仍然正确**） | 扩展：等级/经验/心值/数量/每日剩余 的新框 |
| `src/tasks/daily/GiftTask.py` | 自动赠礼；已能 OCR 等级「10」判满级、OCR `(\d+)/3` 次数 | **接入：读赠礼页快照 + 记消耗** |
| `src/ui/GiftManagerTab.py` | 单页编辑器（左角色列表 / 右设置卡 + 礼物优先级卡） | **加两块 UI** |
| `src/config.py` | `GiftTask` 已注册日常任务，`GiftManagerTab` 已注册标签页 | 不用改注册 |
| `tests/TestGiftManager.py` / `TestGiftTask.py` | 已有回归测试（tempdir + 单例重置模式） | 扩展 |

---

## 2. 截图实测（`C:\tool\ok-nte\data\apps\ok-nte\working\screenshots\*.png`，1920x1080 客户区）

赠礼页**直接显示了全部需要的数据**，不用手填、也不用维护库存表：

- **好感度等级**：大数字（1/2/3/4/8/9/10），带进度环。
- **当前级经验/需求**：如 `1550/12000`、`5765/16000`、`500/2000`。
- **每个礼物格的经验值**（心形数字，实测见到 `100` / `400`）。
- **每个礼物格的库存数量**（角标数字，实测 2/28/6/3/65/50/126/510/301/16…）。
- **特殊礼物标记**：选中带 `CD` 角标的礼物时，底部横幅变
  「该礼物为特殊礼物，不进入每日赠礼次数限制」。
- **每日剩余次数**：底部横幅「今日还能赠送10次礼物」；按钮「赠送3/3」。

### 2.1 经验表（实测 vs 用户给的表）——**需要修正**

| 升级 | 实测截图 | 用户给的表（按索引） |
| --- | --- | --- |
| 1→2 | **500** | index0=100 / index1=400 |
| 2→3 | **1000** | index2=1000 ✅ |
| 3→4 | **2000** | index3=2000 ✅ |
| 4→5 | **3500** | index4=3500 ✅ |
| 5→6 | ？（缺截图） | index5=5000 |
| 6→7 | ？（缺截图） | index6=7000 |
| 7→8 | ？（缺截图） | index7=9000 |
| 8→9 | **12000** | index8=12500 ❌ |
| 9→10 | **16000** | index9=16000 ✅ |
| 10 | 满级（溢出显示 `220`） | — |

推断：用户表是「按等级索引」（等级 1-9 → index 1-9），只是 **L1 应为 500、L8 应为 12000**，
且多写了一个没用的 index0=100。建议最终表：
`L1=500, L2=1000, L3=2000, L4=3500, L5=5000, L6=7000, L7=9000, L8=12000, L9=16000`。
**需要用户确认 L5/L6/L7，最好补 5/6/7 级截图。**

### 2.2 可复用的坐标（1920x1080 比例，待实现时精调）

- 等级数字：约 `(0.79, 0.20) - (0.83, 0.25)`（圆环中心 0.809, 0.218）。
- 经验 `a/b`：约 `(0.77, 0.255) - (0.85, 0.29)`。
- 礼物主网格：沿用 `layout.gift_first_ratio` + 列距/行距；
  **心值文字**在每格卡片下沿、**数量角标**在卡片右下角（新增两个偏移比例）。
- 每日剩余次数横幅：约 `y 0.69 - 0.71`。

---

## 3. 旧代码（`000送礼好感计算器.py`）审查

核心升级循环（`gain_exp`：按当前等级逐次加经验、边加边升级；5% 判定用「送礼前等级」）**逻辑正确，可直接移植**。要修 4 处：

1. **5% 阈值写死 8**（第 99 行）→ 应参数化为 `特技等级 + 3`（1→≤4 … 5→≤8）。
2. **手写信(2000) 在 Day 0 一次性用完，绕过每日上限**（第 140-145 行）→ 按新规则重写。
3. **每日约会 +200 也乘了 1.05**（第 153 行）→ 加成是「送礼 +5%」，约会大概率不该吃（待确认）。
4. **展示的「总经验缺口」没算加成**（第 188 行）→ 偏高。

另：`int(base * coef)` 是截断；单角色、无持久化、无历史。

---

## 4. 方案

### 4.1 数据模型（`GiftDb` v2，带迁移）

```jsonc
{
  "schema_version": 2,
  "settings": {
    "protagonist_skill_level": 1,   // 1-5, 5% 阈值 = 该值 + 3
    "daily_date_enabled": false,    // 每日约会 +200（是否算见待确认）
    "exp_table": { "1": 500, "2": 1000, "...": 0 }   // 可覆盖, 默认用实测表
  },
  "profiles": {
    "gift_xxx": {
      // ... 现有字段不动 ...
      "bond_level": 8,        // 新增: 羁遇等级（OCR 预填, 可手改）
      "bond_exp": 1550,       // 新增: 当前级已存经验
      "target_level": 10,     // 新增: 目标等级
      "buy_tier": 100         // 新增: 库存耗尽后买哪种档位
    }
  },
  "stock": { "100": 0, "400": 0 },          // 新增: 按经验档位的库存（OCR 快照）
  "stock_snapshot_at": "2026-09-22T19:56",  // 新增: 快照时间
  "ledger": {                                // 新增: 每日消耗
    "2026-09-22": { "sent": { "400": 2, "100": 1 }, "characters": { "残虹": 3 } }
  }
}
```

v1 库读入时自动补默认值并升版本，`profiles` 原字段不动。

### 4.2 赠礼页读取器（新 `src/gifts/GiftPageReader.py`）

复用 `GiftTask` 的 OCR 能力，从一帧赠礼页截图读出：

```python
@dataclass
class GiftPageSnapshot:
    bond_level: int | None
    bond_exp: int | None
    bond_exp_required: int | None
    daily_remaining: int | None
    gifts: list[GiftStock]      # 每格: 经验值 + 数量 + 是否特殊

def read_snapshot(task) -> GiftPageSnapshot: ...
def read_character_bond(task) -> tuple[int, int, int] | None: ...
```

- 按**经验档位聚合**库存（同档位的礼物可互换），避开「不同角色格位顺序不同」的问题。
- 局限：只读得到当前可见格位，若列表可滚动且更长，会漏掉下方礼物（见待确认）。

### 4.3 纯计算核心（新 `src/gifts/AffinityCalculator.py`）

```python
EXP_TABLE = {1: 500, 2: 1000, 3: 2000, 4: 3500, 5: 5000, 6: 7000, 7: 9000, 8: 12000, 9: 16000}
MAX_LEVEL = 10
def bonus_threshold(skill_level) -> int: return min(8, skill_level + 3)
def gift_gain(base_exp, level, threshold) -> int: ...   # 带/不带 ×1.05

def simulate_character(...) -> CharacterPlan   # 需几天 / 各档消耗 / 各档需买 / 逐日明细
def simulate_all(profiles, settings, stock) -> GlobalPlan
```

- `simulate_all`：多角色共享库存，逐日轮转；库存按「高经验优先」取用，耗尽按该角色
  `buy_tier` 购买；产出「各档每日消耗 / 库存耗尽在第几天 / 全部达标需几天 / 需买总数」。

### 4.4 消耗记账

- **实际**：`GiftTask` 运行时「开始前读快照 → 送完再读快照 → 差值 = 本次消耗」，
  写入 `ledger[今天]`，并刷新 `stock`。这正好用上用户说的「送完再 OCR 确认真实库存」。
- **计划**：`simulate_all` 的预测消耗，与实测并列展示，便于对照。

### 4.5 UI（扩展 `src/ui/GiftManagerTab.py`，不新建标签页）

- **库存预警看板**（左栏下方紧凑卡）：
  各档位「库存 / 今日消耗 / 可撑天数」，加一行「全部达标还需 N 天，需买 X 个」。
- **好感升级计算器**（右栏）：
  - 设置卡新增「主角特技等级」下拉（1-5）。
  - 角色卡新增「当前等级 / 当前经验 / 目标等级 / 后续购买档位」，
    并提供「从游戏读取」按钮（OCR 预填）。
  - 结果区（只读文本，仿旧版报告）：还需 N 个礼物、需 D 天、加成是否生效。
- 字符串走 `self.tr()`，同步 i18n。

---

## 5. 实施步骤（建议 7 步）

1. **数据模型 v2 + 迁移 + 单测**（`GiftDb` / `GiftManager`）。
2. **计算核心 + 单测**（`AffinityCalculator`：经验表、5% 阈值、单角色、多角色共享库存、剩余天数）。
3. **赠礼页读取器 + 单测**（用 7 张截图做本地回归，缺图自动跳过；截图不入库）。
4. **`GiftTask` 接入 + 单测**（快照差分记账、刷新库存；可选自动读等级）。
5. **UI 库存预警看板**。
6. **UI 好感升级计算器**（每角色）。
7. **i18n + 文档 + 全量 `unittest discover`**。

（5、6 可合并；3、4 可合并。）

---

## 6. 待确认问题

1. **经验表**：是否按上面修正为 `L1=500 … L8=12000`？**L5/L6/L7 是多少**（能否补 5/6/7 级截图）？
2. **每日次数**：底部「今日还能赠送10次礼物」是**全局每日上限 10**吗？每角色是否另有 3 次上限
   （按钮「赠送3/3」）？两者都要在计算里体现吗？
3. **礼物档位**：经验值只有 `100` 和 `400` 两种吗（旧代码还有 200/2000）？哪些是「特殊礼物（不占次数）」？
   哪些能在商店买到、价格/兑换方式是什么？
4. **每日约会 +200** 还要不要算？它享受 5% 加成吗？
5. **礼物列表可滚动吗**？若可滚动，库存是否要滚动读完（更准但慢）还是只读首屏（快但可能少算）？
6. **主角特技等级** 1-5、满级 5、阈值 = 等级 + 3，确认无误？
7. **UI**：确认扩展「羁遇赠礼」标签页（不新建）。
8. **库存扣减**：确认走「OCR 读真实库存」而不是本地累减（用户倾向 OCR，已确认）。
