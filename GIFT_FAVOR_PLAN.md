# 送礼 / 好感度功能改造方案（待确认）

目标：在现有「羁遇赠礼」标签页上，新增
1. **库存预警看板**：记录每日送礼消耗，自动算「当前库存还能送几天」。
2. **好感升级计算器**：设置主角特技等级（1-5，5% 加成阈值 = 特技等级 + 3），
   在角色单独页面选「后续买哪种礼物」，自动算「还要买多少个才能到目标等级」。

本文只是方案，未动代码。末尾「待确认问题」需要用户补充信息后再开工。

---

## 1. 现有实现侦察

| 位置 | 作用 | 与本需求的关系 |
| --- | --- | --- |
| `src/gifts/GiftDb.py` | JSON 持久化（`schema_version=1`），`profiles` 字典；含 `normalize/validate/load/save` | **要扩 schema 到 v2**（加全局设置 + 每角色等级字段） |
| `src/gifts/GiftManager.py` | 单例；`create/recapture/update/delete_profile`、帧图片落盘、`get_enabled_profiles()` | 直接复用；新增字段的读写走这里 |
| `src/gifts/layout.py` | 赠礼页所有坐标比例（角色名、礼物格、次数框、发送按钮） | 复用；新增「等级数字」框比例 |
| `src/tasks/daily/GiftTask.py` | 自动赠礼：进赠礼页 → 滚侧栏匹配角色模板 → 按 `selected_slots` 优先级送礼物 → 读 `(\d+)/3` 次数确认 | **要接入记账**（送成功后写消耗）；已能 OCR 等级「10」判满级 |
| `src/ui/GiftManagerTab.py` | 单页编辑器：左侧角色列表 + 启停，右侧设置卡（名称/次数/启停）+ 礼物优先级卡片 | **要在这里加两块 UI**（看板 + 计算器） |
| `src/config.py` | `GiftTask` 已注册为日常任务，`GiftManagerTab` 已注册为自定义标签页 | 无需改注册（除非新建标签页） |
| `tests/TestGiftManager.py` / `TestGiftTask.py` | 已有回归测试（含 tempdir + 单例重置模式） | 扩展 |
| `gift_configs/` | 目前只有空 `frames/`（用户还没采集角色） | 注意 schema 迁移要能读空库 |

**关键限制**：现有数据里礼物只按「首屏格位 + 截图模板」识别，**没有经验值信息**。
要做「按档位记录消耗 / 算还需买几个」，必须给礼物补上「经验档位」这一层信息。

---

## 2. 旧代码（`000送礼好感计算器.py`）审查

结论：**核心升级逻辑可以直接移植**（`gain_exp` 的「按当前等级逐次加经验、边加边升级」是正确的，
5% 判定用「送礼前等级」也对）。但有 4 个隐藏问题要修：

1. **5% 阈值写死 8**（第 99 行 `self.lvl <= 8`）。按用户描述应为 `特技等级 + 3`
   （1→≤4, 2→≤5, 3→≤6, 4→≤7, 5→≤8）。移植时必须参数化。
2. **手写信(2000) 在「Day 0」一次性全用完，完全绕过每日 3 次上限**（第 140-145 行）。
   如果手写信也占用每日赠送名额，这就是错的（需用户确认）。
3. **每日约会 +200 也乘了 1.05**（第 153 行走 `gain_exp(200)`）。但加成描述是
   「**送礼**额外 +5% 好感」，约会大概率不该吃这个加成（需用户确认）。
4. **「总经验缺口」展示忽略 5% 加成**（第 188 行直接求和经验表），会偏高、和实际所需对不上。

另外两个小点：
- `int(base * coef)` 是截断。目前档位 100/200/400/2000 × 1.05 都是整数，暂无影响；
  若以后有非整除档位，要确认游戏是截断还是四舍五入。
- 单角色、无持久化、无历史，无法直接支撑「看板 / 多角色共享库存」。

---

## 3. 设计方案

### 3.1 数据模型（`GiftDb` v2，带迁移）

```jsonc
{
  "schema_version": 2,
  "settings": {
    "protagonist_skill_level": 1,      // 1-5, 决定 5% 阈值 = 该值 + 3
    "daily_gift_limit": 3,             // 每角色每日赠送次数上限
    "daily_date_enabled": true,        // 每日约会 +200
    "stock": { "100": 0, "200": 0, "400": 0, "2000": 0 }   // 按档位的库存
  },
  "profiles": {
    "gift_xxx": {
      // ... 现有字段保持不变 ...
      "slot_tiers": { "7": 400, "2": 200 },  // 新增: 采集格位 -> 经验档位
      "bond_level": 5,        // 新增: 羁遇等级
      "bond_exp": 0,          // 新增: 当前级已存经验
      "target_level": 10,     // 新增: 目标等级
      "buy_tier": 100         // 新增: 库存耗尽后买哪种档位
    }
  },
  "ledger": {                    // 新增: 每日消耗记录
    "2026-09-22": { "sent": { "400": 2, "200": 1 }, "characters": { "安魂曲": 2 } }
  }
}
```

- 旧库（v1）读入时自动补默认值并升版本，`profiles` 原字段不动，保持向后兼容。
- 需要用户确认：**经验档位只有 100/200/400/2000 四种吗？** 是否所有礼物都能买？

### 3.2 纯计算核心（新文件 `src/gifts/AffinityCalculator.py`）

不碰 UI / 不碰游戏，全部纯函数，便于单测：

```python
EXP_TABLE = [100, 400, 1000, 2000, 3500, 5000, 7000, 9000, 12500, 16000]
MAX_LEVEL = len(EXP_TABLE)              # 10

def bonus_threshold(skill_level): ...   # = min(8, skill_level + 3)
def gift_gain(base_exp, level, threshold, ...): ...   # 带/不带 1.05

def simulate_character(level, exp, target, *, stock, buy_tier,
                       skill_level, daily_limit, date_enabled) -> CharacterPlan
def simulate_all(profiles, settings) -> GlobalPlan
```

- `simulate_character`：移植旧 `gain_exp` 的逐日循环，返回
  「需几天 / 各档位消耗 / 各档位需购买 / 最终等级与溢出经验 / 逐日明细」。
- `simulate_all`：多角色共享库存，逐日轮转每个启用角色（每日约会上限 → 3 次送礼），
  库存按「高经验优先」取用，耗尽则按该角色的 `buy_tier` 购买；
  产出全局「各档位每日消耗 / 库存耗尽在第几天 / 全部达标需几天 / 需买礼物总数」。

### 3.3 消耗记录（账本）

两种来源，建议都做（可配置）：
- **计划消耗**：`simulate_all` 算出的每日消耗（预测）。
- **实际消耗**：`GiftTask` 每次赠送成功后上报 `(角色, 档位)` 写入 `ledger[今天]`，
  并可选自动扣减 `stock`（带去重保护，避免同一单重复扣）。

档位从 `profile["slot_tiers"]` 取（`_find_gift_box` 已经能定位到当前格位，
再把档位一起返回即可）；没打过标签的记为 `unknown`，只统计总数。

### 3.4 UI（扩展 `src/ui/GiftManagerTab.py`，不新建标签页）

- **库存预警看板**（左栏下方紧凑卡）：各档位「库存 / 今日消耗 / 可撑天数」，
  加一行「预计全部达标还需 N 天，需买礼物 X 个」。
- **好感升级计算器**（右栏）：
  - 设置卡新增：主角特技等级（1-5 下拉）。
  - 角色卡新增：当前羁遇等级 / 当前已存经验 / 目标等级 / 后续购买档位。
  - 结果区（只读文本，仿旧版报告）：还需 N 个礼物、需 D 天、加成是否生效。
- 所有字符串走 `self.tr()`，同步 i18n。

### 3.5 自动化接入（`GiftTask`）

- 赠送成功后上报档位并记账（可选扣库存）。
- 可选：采集角色时顺带 OCR 羁遇等级（现在已经有「10 判满级」的框位经验），
  预填 `bond_level`；识别失败则保留手填。**需用户确认是否要自动读等级**。

---

## 4. 实施步骤（建议 7 步）

1. **数据模型 v2 + 迁移 + 单测**（`GiftDb` / `GiftManager`）。
2. **计算核心 + 单测**（`AffinityCalculator`：经验表、5% 阈值、单角色、多角色共享库存、剩余天数）。
3. **账本 + 单测**（每日消耗、库存扣减、去重）。
4. **`GiftTask` 接入 + 单测**（上报档位、记账；可选自动读等级）。
5. **UI 库存预警看板**。
6. **UI 好感升级计算器**（每角色）。
7. **i18n + 文档 + 全量 `unittest discover`**。

（第 5、6 步可合并为一个 UI 步；第 3、4 步也可合并。）

---

## 5. 待确认问题

1. **礼物档位**：只有 100 / 200 / 400 / 2000 四种吗？名称分别是什么？哪些能在商店买到？
2. **手写信(2000)**：占用每日 3 次赠送名额吗？还是独立不占额度？
3. **每日约会 +200**：享受「送礼 +5%」加成吗？
4. **等级映射**：羁遇等级最低是几级？经验表 `[100, 400, ...]` 的第一个 `100` 是
   「0→1 级」还是「1→2 级」？（即表是否按 `level` 直接索引）最大是 10 级吗？
5. **等级/经验来源**：手动输入就够，还是要自动 OCR？（若要自动，请给一张
   角色羁遇页截图，标出等级数字和当前经验的位置）
6. **消耗来源**：看板用「计算器预测」还是「实际运行记录」，还是两者都要？
7. **库存扣减**：`GiftTask` 实跑后要不要自动扣库存（可关）？
8. **多角色共享库存**的取用顺序：按角色列表顺序、高经验优先，可以吗？
9. **UI 位置**：扩展现有「羁遇赠礼」标签页（推荐），还是新开一个标签页？
